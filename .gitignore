node_modules
.next
out
dist
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel
.turbo
coverage
next-env.d.ts
tsconfig.tsbuildinfo
supabase/.branches
supabase/.temp

# Browser verification output (CI uploads these as short-lived artifacts)
playwright-report/
test-results/
lighthouse-report/

# Design export zip is a derived artifact (source lives in design-export/)
design-export.zip

# Round packs are derived artifacts (source dirs are committed)
for-design.zip
for-code.zip
live-screenshots.zip

# Claude Code local (machine-specific) settings
.claude/settings.local.json
